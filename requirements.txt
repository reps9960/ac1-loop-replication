# This project intentionally uses only the Python standard library.
# It talks to any OpenAI-compatible /chat/completions endpoint via urllib.
# No third-party packages are required to run the experiments.
